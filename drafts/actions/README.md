# Draft action schemas (NOT installed)

Destructive verbs are documented here for Stage D design only.

**Do not import from the installable `campaignfuse` package.**

Reserved verbs: `IsolateHost`, `KillPid`, `AddFirewallRule`.

Required fields when Stage D lands: `impact_class`, `dry_run`, `idempotency_key`, `expiry`, `policy_version`.
