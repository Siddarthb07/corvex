# Pre-Stage-B gates

1. `reports/stageA-gate.txt` == PASS
2. `reports/stranger_dry_run.md` completed
3. Then create `reports/stage-b-allowed`

Habit-loop metric (Stage B PASS): external operator produces correct timeline from a scripted purple run without author help.

JetStream + mTLS: see `campaignfuse/sensors/jetstream_mtls.py` (stub until nats-py + certs).
